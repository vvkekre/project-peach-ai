import { ProfilerRequest } from "./rest-api";
import { Account } from "./account";
import { ProgressHandler } from "./progress";
import { CancelToken } from "./cancel";
export interface ProfilerOptions {
    onProgress?: ProgressHandler;
    cancelToken?: CancelToken;
    account?: Account;
    /**
     * Specifies the interval (in milliseconds) at  which the server is polled for a profile ready for download
     * @default 2000
     * */
    pollInterval?: number;
}
export declare const loadProfile: (request: ProfilerRequest, options?: ProfilerOptions | undefined) => Promise<Uint8Array>;
