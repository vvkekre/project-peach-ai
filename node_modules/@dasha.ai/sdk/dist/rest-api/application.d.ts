import { Account } from "../account";
import { Logger } from "../log";
import * as base from "./base";
import * as api from "./generated/core/api";
import * as models from "./generated/core/models";
export interface ApplicationPackage {
    content: Uint8Array;
    contentType: string;
}
export declare class ApplicationApi extends base.Api<api.CustomerApplicationApi> {
    constructor(account: Account, log: Logger);
    list(): Promise<models.CustomerApplicationResponseDTO[]>;
    getById(id: string): Promise<models.CustomerApplicationResponseDTO>;
    getPackageById(id: string): Promise<ApplicationPackage>;
}
