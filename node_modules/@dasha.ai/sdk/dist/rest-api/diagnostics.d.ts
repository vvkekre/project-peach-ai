import { Account } from "../account";
import { Logger } from "../log";
import * as base from "./base";
import * as api from "./generated/core/api";
import * as models from "./generated/core/models";
export declare class DiagnosticsApi extends base.Api<api.DiagnosticsApi> {
    constructor(account: Account, log: Logger);
    getLimits(): Promise<models.LimitsDTO>;
    getInstanceLimits(request: {
        instanceId: string;
    }): Promise<models.LimitsDiagnostic>;
    getQueueStates(): Promise<models.QueuesDTO>;
}
