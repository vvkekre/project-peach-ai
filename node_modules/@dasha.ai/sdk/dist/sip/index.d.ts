import { Connection } from "../connection";
export * as inboundConfigs from "./inbound-configs";
export * as outboundConfigs from "./outbound-configs";
/**
 * @deprecated use the `channel` argument to {@link Conversation.execute} instead
 */
export declare function connect(endpoint: Endpoint): Connection;
/**
 * @deprecated use {@link Conversation.sip} instead
*/
export declare class Endpoint {
    constructor(name: string, options?: {
        noiseVolume: number;
        [key: string]: unknown;
    });
}
export interface SipConfig {
    /**
     * @default "default"
     */
    config: string;
}
