import * as grpc from "@grpc/grpc-js";
import * as base from "./base";
import * as errors from "../errors";
import { Account } from "../account";
import { Logger } from "../log";
import * as services from "./generated/call-gateway_grpc_pb";
import * as messages from "./generated/call-gateway_pb";
export declare enum AudioEncoding {
    LINEAR16 = 0
}
export declare class DuplexAudioStream extends base.Stream<{
    end: [];
    error: [error: errors.Error];
    audio: [streamId: number, time: number, data: Uint8Array];
}, grpc.ClientDuplexStream<messages.AudioBidirectionalStreamRequest, messages.AudioSegment>> {
    writeInfo(info: {
        roomId: string;
        encoding: AudioEncoding;
        sampleRate: number;
    }): void;
    writeAudio(data: Uint8Array): void;
}
export declare class AudioStreamProtocolClient extends base.Client<services.AudioBidirectionalStreamProtocolClient> {
    constructor(account: Account, log: Logger);
    audioStream(): DuplexAudioStream;
}
